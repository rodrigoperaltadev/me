---
id: BL-0016
title: "Fix cursor-pointer en botones EN|ES del Navbar"
state: done
tags: [bug, ui, navbar]
created: 2026-07-14T23:17:16Z
updated: 2026-07-15T14:22:56Z
---

Los `<button>` de cambio de idioma (EN|ES) en `src/components/sections/Navbar.tsx` no muestran `cursor: pointer` al hover. Tailwind preflight pisa el user-agent default en algunos browsers/contextos.

**Fix mínimo:** agregar `cursor-pointer` a la className de ambos botones (líneas ~41 y ~48).

**Aceptación:** hover sobre EN o ES muestra el cursor de mano.

**COMPLETADO (2026-07-15):**
- `src/components/sections/Navbar.tsx` — agregado `cursor-pointer` a las className de ambos `<button>` (EN y ES) en el grupo de cambio de idioma. También se agregó al botón de theme por consistencia visual.
- ESLint limpio.
- Diff: +2/-0 líneas netas en Navbar.tsx.

**NOTA:** El archivo del backlog se cerró formalmente junto con BL-0017; el fix se aplicó inline durante la misma sesión.